using System;
using System.Collections.Generic;
using System.Text;

namespace AjaxControlToolkit {
    /// <summary>
    /// The positioning choices for the char count popup
    /// </summary>
    public enum CharCountPosition {
        BottomLeft = 0,
        BottomRight = 1,
        TopLeft = 2,
        TopRight = 3,
        Right = 4,
        Left = 5
    }
}
